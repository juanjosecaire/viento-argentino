---
description: Publicar el sitio Viento Argentino — SEO, enlaces, GitHub y Vercel
globs: "**/*.html,vercel.json,README.md"
alwaysApply: false
---

# Publicación — Viento Argentino

El usuario publica desde **este chat** (o uno dedicado a deploy). Al pedir publicar, desplegar o subir cambios, seguí este flujo completo.

## Infraestructura

```text
Código local → git push (rama main) → GitHub → Vercel (deploy automático) → vientoargentino.com
```

| Pieza | Detalle |
|-------|---------|
| **Repositorio** | `https://github.com/juanjosecaire/viento-argentino.git` |
| **Rama de producción** | `main` |
| **Hosting** | Vercel (sitio estático, sin build) |
| **Dominio** | `vientoargentino.com` y `www.vientoargentino.com` |
| **Config Vercel** | Framework **Other**, Build Command vacío, Output Directory `.` |
| **URLs limpias** | `vercel.json` con `"cleanUrls": true` |

## Estructura actual del sitio

- `index.html` — potencial eólico, inicio del sitio (`/`)
- `green-ammonia-hub.html` — programa Patagonia Green Ammonia Hub (`/green-ammonia-hub`)
- `se-hace-camino-al-andar.html` — proyecto Se hace camino al andar (`/se-hace-camino-al-andar`)
- `documentos/` — PDFs publicados en producción

Redirección permanente: `/potencial-eolico` → `/`.

Marca visible: **Viento Argentino**. Cada página enlaza su PDF en `documentos/` (usar `%20` en URLs si el nombre tiene espacios).

## Checklist antes de cada publicación

1. **Cambios listos** — Revisar diff; solo archivos del sitio.
2. **Nueva página HTML** (si aplica):
   - Añadir `<title>` y `<meta name="description">` únicos en español, alineados al dominio y al contenido de la página.
   - Formato sugerido del title: `Tema de la página | Viento Argentino`.
   - Description: 1–2 frases concretas (150–160 caracteres aprox.), sin relleno genérico.
   - Actualizar navbar y footer en **todas** las páginas existentes con enlace a la nueva ruta.
3. **Enlaces** — Verificar en todas las páginas:
   - Brand → `/` o `index.html`
   - Enlaces internos coherentes con `cleanUrls` (p. ej. `green-ammonia-hub.html` o `/green-ammonia-hub`)
   - Sin enlaces rotos a `potencial-eolico.html`, `nuevo0.html`, `nuevo1.html` ni rutas obsoletas
   - PDFs accesibles: `documentos/Potencial%20viento%20patagonia.pdf` (portada `/`), `documentos/Patagonia%20Green%20Ammonia%20Hub.pdf` (hub) y `documentos/Potencial%20E%C3%B3lico%20Patagonia%20(3).pdf` (camino al andar)
   - Botones del hero y footer apuntan a secciones o páginas válidas
4. **Git** — Solo commitear si el usuario lo pide explícitamente:
   ```bash
   git add .
   git commit -m "mensaje claro del cambio"
   git push origin main
   ```
5. **Post-push** — Recordar que Vercel despliega solo; sugerir comprobar el deployment en el dashboard y probar producción:
   - `https://vientoargentino.com/`
   - `https://vientoargentino.com/green-ammonia-hub` (y cualquier página nueva)

## Meta tags — ejemplo

**Portada (`index.html`, potencial eólico):**
```html
<title>Viento Argentino | Potencial eólico en la Patagonia</title>
<meta name="description" content="Evaluación del recurso eólico en la Patagonia argentina: 9.600 TWh anuales, equivalencia con petróleo, comparación con Vaca Muerta, el PBI y la energía nuclear mundial.">
```

**Hub (`green-ammonia-hub.html`):**
```html
<title>Patagonia Green Ammonia Hub | Viento Argentino</title>
<meta name="description" content="Propuesta para transformar el viento patagónico en hidrógeno verde, amoníaco verde y fertilizantes ecológicos...">
```

## DNS (solo si el dominio falla)

En Namecheap → Advanced DNS, los valores deben coincidir con lo que muestra Vercel en **Settings → Domains** del proyecto (IP y CNAME pueden cambiar por proyecto). Refrescar dominio en Vercel tras actualizar DNS.
